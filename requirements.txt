# KakaoCloud Essential 문제은행 — Python 패키지 목록
# Python 3.10 이상 필요
# 설치: pip install -r requirements.txt

# HTTP 요청 (parse-kakao-docs.py)
requests>=2.31.0
beautifulsoup4>=4.12.0   # HTML 파싱

# Markdown 파싱 (validate-quality.py, build-exam.py)
python-frontmatter>=1.1.0  # YAML frontmatter 읽기/쓰기

openai>=1.0.0
anthropic>=0.39.0

# 환경변수 관리 (모든 스크립트)
python-dotenv>=1.0.0

# CLI 인터페이스 (모든 스크립트)
click>=8.1.0

# 유틸리티
pathlib2>=2.3.7; python_version < "3.10"  # Python 3.10 미만 대응
