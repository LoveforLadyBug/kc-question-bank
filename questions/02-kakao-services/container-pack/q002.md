---
id: q002
chapter: 02-kakao-services/container-pack
topic: cluster
difficulty: 2
type: single
tags: [cluster, control-plane, vpc, managed-kubernetes]
source: https://docs.kakaocloud.com/service/container-pack/k8se/k8se-overview
status: draft
created: 2026-04-27
reviewed_by: []
quality_score: ~
---

## 문제

카카오클라우드 Kubernetes Engine의 클러스터 구성에 대한 설명으로 옳은 것은?

## 보기

- A. 제어 영역(마스터 노드)과 워커 노드 모두 사용자의 VPC에 배포된다.
- B. 제어 영역은 카카오클라우드가 관리하는 별도 VPC에서 운영되고, 워커 노드는 사용자 VPC에 배포된다.
- C. 제어 영역은 사용자가 직접 설정하고, 워커 노드는 카카오클라우드가 자동으로 관리한다.
- D. 클러스터 생성 시 제어 영역과 워커 노드는 동일한 서브넷에 위치해야 한다.

## 정답

B

## 해설

카카오클라우드 공식 문서에 따르면, Kubernetes Engine의 제어 영역(Control Plane)은 카카오클라우드가 관리하는 별도의 VPC에서 완전 관리형으로 운영됩니다. 워커 노드는 사용자의 VPC에 배포되며, 사용자는 워커 노드와 그 위에서 실행되는 애플리케이션에만 집중하면 됩니다.

## 오답 포인트

- A: 제어 영역은 사용자 VPC가 아닌, 카카오클라우드가 관리하는 별도의 VPC에 위치합니다. 사용자 VPC에는 워커 노드만 배포됩니다.
- C: 제어 영역은 사용자가 직접 설정하는 것이 아니라 카카오클라우드가 완전 관리합니다. 관리형 서비스의 핵심 특징입니다.
- D: 제어 영역은 카카오클라우드의 별도 VPC에 있고 워커 노드는 사용자 VPC에 있으므로, 동일한 서브넷에 위치한다는 설명은 잘못되었습니다.
